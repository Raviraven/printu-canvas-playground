import { ValidateSvgData, CalculateOutlineValues } from './Utils';

describe('Utils - ValidateSvgData - tests', () => {
  // test.each
  test.todo('should return validation errors');
});

describe('Utils - CalculateOutlineValues - tests', () => {
  test.todo('should calculate proper outline values');
});
