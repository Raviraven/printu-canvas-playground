import { SingleFigure } from './SingleFigure';

describe('SingleFigure tests', () => {
  test.todo('should throw error when trying to generate unknown figure type');

  test.todo('should generate svg figure with outline and label');
});
