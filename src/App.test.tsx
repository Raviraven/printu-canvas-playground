import React from 'react';
import { render, screen } from '@testing-library/react';
import App from './App';

describe('App tests', () => {
  test.todo('should show error boundary component when some error occur');
});
